diff -up PyHyphen-1.0beta1/MANIFEST.in.build PyHyphen-1.0beta1/MANIFEST.in
--- PyHyphen-1.0beta1/MANIFEST.in.build	2012-02-17 02:40:59.741632493 +0900
+++ PyHyphen-1.0beta1/MANIFEST.in	2012-02-17 03:08:19.780573870 +0900
@@ -0,0 +1,11 @@
+# file GENERATED by distutils, do NOT edit
+include README.txt
+include setup.py
+include textwrap2.py
+include hyphen/*.py
+include include/*
+include src/*.c
+include 2.x/*.py
+include 3.x/*.py
+include 2.x/*.c
+include 3.x/*.c
diff -up PyHyphen-1.0beta1/setup.py.build PyHyphen-1.0beta1/setup.py
--- PyHyphen-1.0beta1/setup.py.build	2012-02-17 03:26:01.865535906 +0900
+++ PyHyphen-1.0beta1/setup.py	2012-02-17 03:24:39.159538863 +0900
@@ -1,8 +1,8 @@
 
-import sys, os, shutil, codecs, locale
+import sys, os, os.path, shutil, codecs, locale
 from string import Template
 from distutils.core import setup, Extension
-
+import glob
 
 
 # URL of the default repository. It goes into config.py.
@@ -26,11 +26,20 @@ if not os.path.exists('hyphen'): os.mkdi
 ver = sys.version[0]
 py3k = (ver == '3')
 for file_name, dest in files.items():
-    shutil.copy(ver + '.x/' + file_name, dest + file_name)
-
+    src = os.path.abspath(os.path.join(os.curdir, ver + '.x', file_name))
+    dst = os.path.abspath(os.path.join(os.curdir, dest, file_name))
+    shutil.copy(src, dst)
+
+for f in glob.glob("src/*"):
+    shutil.copy(f, ver + ".x")
 
 
-longdescr = open('README.txt').read()
+longdescr = """
+The gole of the PyHyphen project is to provide Python with a high quality
+hyphenation facility.  PyHyphen consists of the package 'hyphen' and the module
+'textwrap2'.  The source distribution supports Python 2.4 or higher, including
+Python 3.2.
+"""
 
 
 
@@ -138,6 +147,6 @@ if 'install' in sys.argv:
         Could not import hyphen package.
         You may wish to adjust config.py
             manually or run setup.py with different options.
-            No dictionary has been installed.\n""")
-
+            No dictionary has been installed.
+            """)
 
